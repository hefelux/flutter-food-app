import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double carouselWrapperHeight = screenHeight / 2.64;
  static double carouselImageHeight = screenHeight / 3.84;
  static double carouselDesciptionHeight = screenHeight / 7.03;

  static double height10 = screenHeight / 84.4;
}
